#! /usr/bin/env ruby

require 'pg'

db = PG.connect(dbname: 'expenses')

result = db.exec "SELECT * FROM expenses;"

result.each do |row|
  puts "#{row['id']} | #{row['created_on']} | #{row['amount']} | #{row['memo']}"
end
